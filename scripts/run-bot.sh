#!/bin/bash

# Скрипт для запуска бота из директории bot/
# Используется для запуска бота как отдельного процесса

cd "$(dirname "$0")/../bot" || exit 1

# Запускаем встроенный PHP сервер
php -S 0.0.0.0:${PORT:-8000} -t .
